description: >
  Validate all zones in a dir
parameters:
  path:
    type: string
    default: "."
    description: "Path of zone files to validate"
steps:
  - run:
      name: Validate zones
      command: |
        <<include(src/scripts/validate_zones.py)>> <<parameters.path>>
