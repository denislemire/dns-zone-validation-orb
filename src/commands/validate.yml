description: >
  Validate all zones in a dir
parameters:
  path:
    type: string
    default: "."
    description: "Path of zone files to validate"
steps:
  - run:
      name: Validate zones
      environment:
        DNS_ZONE_PATH: <<parameters.path>>
      command: |
        <<include(src/scripts/validate_zones.py)>>
